name: THRESHOLD
on:
  push:
    branches: [main]
    paths:
      - "THRESHOLD/request.txt"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  respond:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install requests
      - run: python THRESHOLD/scripts/threshold_encounter.py
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      - name: Commit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "museum-bot"
          git config user.email "museum-bot@users.noreply.github.com"
          git add THRESHOLD/response.txt THRESHOLD/visits.json museum-state.json journey/
          git diff --staged --quiet || git commit -m "THRESHOLD: response generated"
          git pull --rebase -X theirs https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
