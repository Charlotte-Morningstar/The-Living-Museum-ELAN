name: Depths System
on:
  push:
    branches: [main]
    paths:
      - "DEPTHS/request.txt"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  respond:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install requests

      - run: python DEPTHS/scripts/depths_encounter.py
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Commit response
        run: |
          git config user.name "museum-bot"
          git config user.email "museum@living-museum"
          git add DEPTHS/response.txt
          git add DEPTHS/visits.json
          git add DEPTHS/history.json
          git add museum-state.json
          git add journey/
          git diff --staged --quiet || git commit -m "Depths: journey measured"
          git pull --rebase -X theirs origin main || true
          git push
